package main

import (
	"fmt"

	"github.com/stepup99/microservices-p1/mvc/utils"
)

func main() {
	// app.StartApp()
	// fmt.Println(utils.BubbleSort([]int{4, 5, 6, 7, 1, 2}))
	// fmt.Println(utils.GenerateArr(10))
	// elms := utils.GenerateArr(10)
	fmt.Println(utils.InbuiltSort([]int{4, 5, 6, 7, 1, 2}))
}
